pipeline {
    agent { label "jenkins-s3-ansible"}
    
    stages {
        stage("SCM checkout") {
            steps {
                sh """
                    pwd
                    ansible test -m ping
                """
            }
        }
    }
}
