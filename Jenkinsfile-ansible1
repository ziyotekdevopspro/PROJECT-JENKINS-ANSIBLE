pipeline {
    agent { label "jenkins-s3-ansible"}
    
    stages {
        stage("SCM checkout") {
            steps {
                sh """
                    ansible test -m ping
                """
            }
        }
    }
}
