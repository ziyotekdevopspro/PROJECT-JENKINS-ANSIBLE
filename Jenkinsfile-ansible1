pipeline {
    agent { label "jenkins-s3-ansible"}
    
    stages {
        stage("Run Ansible adhoc") {
            steps {
                sh """
                    ansible -i inventory test_server -m ping
                """
            }
        }
    }
}
