pipeline {
    agent {
        node {
            label "jenkins-s3-ansible"
        }
    }
    stages {
        stage("SCM checkout") {
            steps {
                git 'https://github.com/ziyotekdevopspro/PROJECT-JENKINS-ANSIBLE.git'
            }
        }
        stage("SCM checkout") {
            steps {
                git 'https://github.com/ziyotekdevopspro/PROJECT-JENKINS-ANSIBLE.git'
            }
        }
    }
}